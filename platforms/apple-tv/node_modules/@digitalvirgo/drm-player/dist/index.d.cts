interface DrmPlayerConfig {
    serviceId: string;
    authLogin?: string;
    authSecret?: string;
    authHost?: string;
    smartVideoHost?: string;
    drmEnv?: string;
    castlabsLicense?: string;
}
declare function configure(config: DrmPlayerConfig): void;

interface DrmConfig {
    merchant: string;
    userId: string;
    sessionId: string;
    assetId: string;
    authToken?: string;
}
interface PlayRequest {
    url: string;
    poster?: string;
    drm?: DrmConfig;
    autoplay?: boolean;
    muted?: boolean;
    loop?: boolean;
}
interface PlayerState {
    playing: boolean;
    paused: boolean;
    currentTime: number;
    duration: number;
    loading: boolean;
    error: string | null;
    ended: boolean;
}

declare function usePlayer(): {
    containerRef: (node: HTMLDivElement | null) => void;
    state: PlayerState;
    play: (request: PlayRequest) => Promise<void>;
    pause: () => void;
    resume: () => void;
    seek: (seconds: number) => void;
    destroy: () => void;
};

interface User {
    id: string;
    email?: string;
    msisdn?: string;
    firstname?: string;
    lastname?: string;
    subscribed: boolean;
    token?: string;
}
interface SmartVideoConfig {
    stream: string;
    sessionId: string;
    drm: {
        stream: string;
        dwn_high: string;
        dwn_low: string;
    };
    assets: {
        stream: string;
        dwn_high: string;
        dwn_low: string;
    };
    drm_end: string;
    drm_view: number;
}

declare function getSmartVideoDrmConfig(params: {
    userId: string;
    galaxyRef: number;
    tokenUrl: string;
    orderId: string;
    isKlientoUser?: boolean;
    country?: string;
}): Promise<SmartVideoConfig>;

/**
 * Replicates the Android `dvHash` password hashing.
 * hash = SHA1(salt[0..10] + password + salt[10..32])
 */
declare function dvHash(password: string): Promise<string>;
declare function loginWithEmail(email: string, password: string): Promise<User>;
declare function fetchAccountInfo(userId: string): Promise<Partial<User>>;
declare function deliveryOrder(userId: string, contentRef: number, orderType?: number): Promise<{
    orderId: string;
} | null>;

declare const PLAYER_CONTAINER_CLASS = "player-container";

export { type DrmConfig, type DrmPlayerConfig, PLAYER_CONTAINER_CLASS, type PlayRequest, type PlayerState, type SmartVideoConfig, type User, configure, deliveryOrder, dvHash, fetchAccountInfo, getSmartVideoDrmConfig, loginWithEmail, usePlayer };
